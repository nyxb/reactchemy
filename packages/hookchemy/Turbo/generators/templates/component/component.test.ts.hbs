import { {{pascalCase name}} } from './{{camelCase name}}'

describe('{{name}}()', () => {
test('should {{name}} be ok', () => {
const { result } = renderHook(() => {{pascalCase name}}())
const [value, method] = result.current

expect(value).toBe(2)
expect(typeof method).toBe('function')
})

test('should method returns 2', () => {
const { result } = renderHook(() => {{pascalCase name}}())
const [, method] = result.current

let value = 0

act(() => { value = method() })

expect(value).toBe(2)
})
})
